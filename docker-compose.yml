version: '3.9'

services:
  web:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0
    volumes:
      - .:/app
    ports:
      - target: 8080
        x-aws-protocol: http
    image: "deploiement_clement"




